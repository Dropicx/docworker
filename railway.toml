[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.railway.light"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[[services]]
name = "doctranslator"
port = 8080